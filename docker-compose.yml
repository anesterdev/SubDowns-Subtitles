services:
  app:
    build: .
    container_name: subdowns
    env_file:
      - .env
    ports:
      - "${PORT:-3069}:${PORT:-3069}"
      - "${MCP_PORT:-9000}:${MCP_PORT:-9000}"
    restart: unless-stopped
